# 🚀 Quick Start: React Frontend + Django Backend

## ✅ Что было сделано

### Интеграция выполнена:
1. ✅ Django настроен для работы с React SPA
2. ✅ Созданы API endpoints для всех основных функций
3. ✅ Настроен CORS для dev-режима
4. ✅ React приложение готово к работе
5. ✅ URL роутинг настроен корректно

### Структура API:
- `/api/dashboard/*` - метрики дашборда
- `/api/products/*` - управление товарами
- `/api/inventory/*` - складской учет
- `/api/telegram/*` - уведомления
- `/api/export/*` - экспорт данных
- `/admin/*` - Django админка

## 🎯 Запуск приложения (Development Mode)

### Шаг 1: Запустите Django Backend

Откройте **Terminal 1** (PowerShell):

```powershell
# Активируйте виртуальное окружение
.\venv\Scripts\Activate.ps1

# Запустите Django сервер
python manage.py runserver
```

✅ Django запустится на: **http://localhost:8000**

### Шаг 2: Запустите React Frontend

Откройте **Terminal 2** (PowerShell):

```powershell
# Перейдите в директорию frontend
cd frontend

# Установите зависимости (только первый раз!)
npm install

# Запустите Vite dev server
npm run dev
```

✅ React запустится на: **http://localhost:5174** (или 5173)

### Шаг 3: Откройте приложение

Откройте браузер и перейдите на:
- **React App:** http://localhost:5174
- **Django Admin:** http://localhost:8000/admin

## 📋 Первый вход

### 1. Создайте суперпользователя (если еще нет):

```powershell
python manage.py createsuperuser
```

### 2. Войдите в Django Admin:

Перейдите на http://localhost:8000/admin и войдите

### 3. Создайте компанию и назначьте пользователю:

```python
# В Django shell (python manage.py shell):
from accounts.models import Company, User

# Создать компанию
company = Company.objects.create(
    name="Моя компания",
    marketplace_credentials={}
)

# Назначить компанию пользователю
user = User.objects.get(email='your@email.com')
user.company = company
user.save()
```

### 4. Откройте React приложение:

Теперь можно открыть http://localhost:5174 и увидеть React интерфейс!

## 🔍 Проверка что все работает

После входа вы должны увидеть:
- ✅ **Dashboard** с метриками и графиками
- ✅ **Products** - список товаров
- ✅ **Inventory** - складской учет
- ✅ **Settings** - настройки компании

## 🐛 Troubleshooting

### Проблема: "No company assigned"
**Решение:** Назначьте компанию пользователю (см. Шаг 3 выше)

### Проблема: API возвращает 401
**Решение:** Сначала войдите через Django admin: http://localhost:8000/admin

### Проблема: CORS errors
**Решение:** Убедитесь что Django запущен на порту 8000, а React на 5174

### Проблема: "Port 5173 is in use"
**Решение:** Vite автоматически выберет другой порт (5174). Используйте его.

### Проблема: React не загружается
**Решение:** 
1. Проверьте что Vite запущен (`npm run dev` в директории frontend)
2. Проверьте консоль браузера на ошибки
3. Убедитесь что порт в `templates/index.html` совпадает с портом Vite

## 📊 Архитектура

```
┌─────────────┐
│   Browser   │
└──────┬──────┘
       │
       ├─────────────────────────────────┐
       │                                 │
       ▼                                 ▼
┌──────────────┐                 ┌──────────────┐
│ Vite Server  │                 │    Django    │
│ :5174        │◄────proxy───────│    :8000     │
│              │   /api/*        │              │
│ React SPA    │                 │ REST API     │
└──────────────┘                 └──────┬───────┘
                                        │
                                        ▼
                                 ┌─────────────┐
                                 │  Database   │
                                 └─────────────┘
```

## 🎨 Что дальше?

### Наполните базу тестовыми данными:

```powershell
# Создайте склады
python manage.py shell
>>> from sales.models import Warehouse
>>> from accounts.models import Company
>>> company = Company.objects.first()
>>> Warehouse.objects.create(name="Основной склад", location="Москва", company=company, is_active=True)

# Создайте товары
>>> from products.models import Product
>>> Product.objects.create(
    company=company,
    sku="PROD-001",
    name="Тестовый товар",
    price=1000,
    cost=500
)
```

### Проверьте все страницы:
1. ✅ Dashboard - главная страница
2. ✅ Products - каталог товаров
3. ✅ Inventory - складской учет
4. ✅ Settings - настройки

## 🎉 Готово!

Теперь у вас полностью рабочее приложение с React фронтендом и Django бэкендом!

**Для разработки:**
- Изменения в React автоматически обновляются (Hot Module Replacement)
- Изменения в Django требуют перезапуска сервера
- API запросы автоматически проксируются на Django

**Для production:**
```powershell
# Собрать фронтенд
cd frontend
npm run build

# Запустить только Django
python manage.py runserver
```

Django будет отдавать собранный React на http://localhost:8000
